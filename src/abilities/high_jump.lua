-- Stub : saut plus haut (kangourou) — non branché au joueur dans le MVP.

local HighJump = {}

HighJump.id = "high_jump"
HighJump.description = "Saut plus haut (stub — espèce non sélectionnable)."

function HighJump.apply(_player, _input)
  -- Intentionnellement vide : format d'API pour phases suivantes.
  return false
end

return HighJump
